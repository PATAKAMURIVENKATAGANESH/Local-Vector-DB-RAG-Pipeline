This is a repo is an end-to-end Local RAG pipeline which runs completely local on your machine (if you use ollama, else for LLM  you have use to the API keys).
Note: duplicate the .env.sample file and rename it as .env file and replace you API keys
